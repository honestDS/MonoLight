# 项目待办

## 审计二次确认验收

- [x] 已确认工具执行工作发生异常或租约中断时不得自动重试有副作用的工具，应转为 `execution_unknown` 并要求人工处理。
- [x] 强制校验审计模型对直接引用脚本的读取结果；证据缺失、`file_checks` 为空或材料不足时不得低风险放行。
- [x] 整轮预检按工具完整 JSON Schema 校验参数类型、结构和取值范围，不只校验参数名称。
- [x] 将审计记录创建、确认占用和执行领取的条件写入改为 SQLite、MySQL、PostgreSQL 均支持的实现，并分别补充验证。
- [x] 确认卡片状态以服务端终态为准；启动恢复、过期、拒绝、取消和执行完成后均回写并广播最终状态。
- [x] 补充网页、WebSocket、微信端到端确认流程测试；按当前要求不保留前端确认卡片测试。
- [ ] 追加写入目标在审计时不存在时也保存该状态，并在确认执行前核对其是否被创建或替换。
- [ ] 所有执行入口迁移到整轮审计后(应该已经完成 请检查)，删除旧逐工具审计及其兼容调用链。
- [ ] 确认卡片的过期时间应在前端系统配置-配置管理-安全设置选项卡中可配置
