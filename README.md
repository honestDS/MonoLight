# MonoLight

MonoLight 是一个旨在重塑人机交互形态的通用自主智能体（General Autonomous Agent）核心基座。

## 我们的愿景

我们致力于构建一个极致精简且具备高度自主执行能力的底层框架。在集成多模态大模型适配、循环反思决策、物理指令审计、RAG 知识检索、动态 Skill 插件及定时任务等核心 Agentic 功能之上，MonoLight 的终极目标是成为支撑全场景外部接入的统一智能基座。

通过框架内置的多协议适配层，MonoLight 将作为逻辑中枢，深度赋能包括但不限于以下领域：
一 智能交互：构建具备长效记忆与个性化偏好的新一代聊天机器人。
二 自动化运维：实现服务器集群的自主巡检、故障自愈与云原生环境的敏捷开发。
三 具身智能：作为云端大脑节点，赋予机器人、无人设备等物理实体逻辑思考与现实任务执行的统一能力。

我们的图景是打破数字世界与物理世界的隔阂，让 MonoLight 成为连接人类意图与现实执行的通用桥梁。

---

## AI Agent 开发规范守则

> [!IMPORTANT]
> 所有参与本项目贡献的 AI Agent 必须严格遵守以下开发标准与架构原则：
> 1. 阅读并执行 [DEVELOPMENT_GUIDE.md](./DEVELOPMENT_GUIDE.md) 中的命名规范、代码风格（Ruff）及测试要求。
> 2. 参考 [ARCHITECTURE.md](./ARCHITECTURE.md) 以确保符合系统设计与模块依赖关系。
> 3. 参考 [API.json](./API.json) 以确保编写集成测试时接口访问的准确性
> 3. 在提交任何代码前，必须确保通过 `ruff check` 与 `ruff format` 检查。
> 4. 所有测试用例的编写或修改必须严格基于目标代码的实际实现。在编写测试前，AI Agent 必须完整阅读并解析目标源码，确保 Mock 逻辑与业务流转完全对齐，严禁凭经验或假设编写测试代码。
> 5. 如果修改了API接口，必须通过http://服务器IP:端口号/openapi.json 重新拉取最新的文档内容并更新项目跟目录下的API.json文件

## 1. 核心特性
- **多类型数据库支持**: 支持sqlite(默认)、mysql、postgresql数据库。
- **自主 Agent 架构**: 内置 Dispatcher 循环机制，支持 AI 自主调用工具链完成复杂逻辑。
- **多用户系统**: 引入完整的用户权限体系与多角色管理。
- **多用户会话隔离**: 实现用户会话隔离。
- **配置管理中心**: 前后端深度打磨的配置管理页面，支持可视化管理模型参数、采样策略及提供商映射。
- **异常驱动架构**: 引入 StandardResponse 与业务异常体系，通过全局 Handler 实现精准的错误分级与业务文案收口。
- **提示词资产管理**: 独立的 PromptLibrary 支持多套提示词随时切换与复用。
- **物理工具集成**: 首发支持 ShellExecutor，允许 AI 在受控环境下执行系统指令。
- **全异步架构**: 基于 FastAPI (aiohttp) 与 SQLAlchemy 的高性能并发处理。

## 2. 交互入口
- **仪表盘 (Dashboard)**: 基于 Vue 3 + Element Plus 的现代管理后台，提供极致流畅的配置与交互体验。
- **API 文档**: 内置 Swagger (/docs)，支持标准的鉴权与业务接口调用。

## 3. 未来规划
- [x] 完成核心配置及WEB对话流程(HTTP)
- [x] 完成会话历史记录接口
- [x] 解耦dashboard的所有.vue单文件 将逻辑于模板分离
- [x] 完成前端webscoket对话流程
- [x] 完成后端流式请求支持 实现真正的流式对话
- [x] 会话消息追加功能: 实现agent在处理多轮步骤过程中的消息追加功能。
- [x] 自动会话标题生成: 实现新建会话时自动根据会话内容生成标题的功能。
- [ ] **多用户SSH隔离**: 实现服务器操作环境与数据隔离。
- [ ] **计划任务系统**: 支持 Cron 表达式驱动的 Agent 任务定时执行与循环触发。
- [x] **后台实时日志**: 集成 WebSocket 实现服务端执行日志的毫秒级流式下发与可视化审计。
- [x] **深度参数配置化**: 抽离核心业务逻辑参数至统一配置中心，支持热加载与多环境无缝切换。
- [ ] **Skill 动态加载**: 实现技能库的热插拔与在线热更新机制。
- [ ] **向量模型与知识库支持**: 集成 RAG 架构，支持本地/云端向量数据库以增强 Agent 的长效知识索引能力。
- [ ] **重排序模型支持**: 引入 Rerank 机制优化知识库检索精度，显著提升 Agent 对长文本上下文的理解与过滤能力。
- [ ] **更多的上下文压缩机制选择**: 引入滑动窗口[已实现]、摘要压缩及关键信息提取算法，在保持逻辑一致性的前提下最大限度节省 Token 消耗。
- [x] **实现图片、文件传输**: 支持用户上传图片、文件等二进制数据，实现与大模型的直接交互。
- [ ] **接入 QQ平台 消息适配器**: 实现与 QQ平台 的消息交互功能。
- [ ] **新增更多工具**: 文件写入[已实现]、搜索工具

## 4. 技术架构
详细文档请参考 [ARCHITECTURE.md](./ARCHITECTURE.md)

## 5. 开源协议
本项目采用 AGPL-3.0 协议开源。

## 自动化测试
项目已接入自动化测试体系，涵盖单元测试、初始化逻辑测试以及 API 集成测试。

### 执行测试命令
在项目根目录下执行以下命令运行全量测试：

```bash
PYTHONPATH=. pytest tests/
```

### 测试目录说明
1. tests/unit: 核心逻辑单元测试
2. tests/initialization: 数据库初始化与迁移逻辑测试
3. tests/integration: API 异步接口集成测试

